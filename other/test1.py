import maya.cmds as cmds

# selected_objects = cmds.ls(selection=1)
# print selected_objects

# for i in selected_objects:
#     print i 
#     # cmds.setAttr(i +".overrideEnabled", 1)
#     # cmds.setAttr(i +".overrideColor", 17)
    # cmds.setAttr(i +".segmentScaleCompensate", 0)


# n = 100

# while n>0 :
#     sph = cmds.polySphere()
#     cmds.move( n*2, 0, 0 )
#     n = n-1

# for i in selected_objects:
#     loc = cmds.spaceLocator()
#     con = cmds.parentConstraint(i, loc, mo=0)
#     cmds.delete(con)

